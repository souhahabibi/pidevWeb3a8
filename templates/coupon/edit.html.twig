{% extends 'admin_base.html.twig'%}

{% block title %}Modifier un Coupon{% endblock %}

{% block body %}
    <h1>Edit Coupon</h1>

    {{ include('coupon/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_coupon_index') }}">back to list</a>

    {{ include('coupon/_delete_form.html.twig') }}
{% endblock %}
