{% extends 'admin_base.html.twig' %}
{% block body %}
 <div class="container-fluid pt-4 px-4">
    <div class="row g-4" style="padding-left: 50px;">
        <div class="col-sm-12 col-xl-10">
            <div class="bg-secondary rounded h-100 p-4">
                <h5 class="mb-4">Add Competition</h5>
                {{ form_start(f,{'attr': {'novalidate': 'novalidate'}} ) }}
                    <div class="mb-3">
                        {{ form_label(f.nom,"Name") }}
                        {{ form_widget(f.nom) }}
                        {{ form_errors(f.nom) }}
                    </div>
                    <div class="mb-3">
                        {{ form_label(f.date,"date") }}
                        {{ form_widget(f.date) }}
                        {{ form_errors(f.date) }}
                    </div>
                    <div class="mb-3">
                        {{ form_label(f.description,"description") }}
                        {{ form_widget(f.description) }}
                        {{ form_errors(f.description) }}
                    </div>
                    <div class="mb-3">
                        {{ form_label(f.capacite,"capacite") }}
                        {{ form_widget(f.capacite) }}
                        {{ form_errors(f.capacite) }}
                    </div>
                    <div class="mb-3">
                        {{ form_label(f.videourl,"videourl") }}
                        <span class="input-group-text" id="basic-addon3">https://www.youtube.com/embed/.....</span>
                        {{ form_widget(f.videourl) }}
                        {{ form_errors(f.videourl) }}
                    </div>
                    <div class="mb-3">
                        {{ form_label(f.fkOrganisateur,"fkOrganisateur") }}
                        {{ form_widget(f.fkOrganisateur) }}
                        {{ form_errors(f.fkOrganisateur) }}
                    </div>
                    <div style="padding-left: 350px;">
                        {{ form_widget(f.Submit) }}
                    </div>
                {{form_end(f)}}
            </div>
        </div>
    </div>
 </div>       
{% endblock %}