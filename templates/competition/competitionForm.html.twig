{% extends 'admin_base.html.twig' %}
{% block body %}
 <div class="container-fluid pt-3 px-3">
   <div class="row  bg-secondary rounded align-items-center justify-content-center mx-0">
    {{ form_start(f,{'attr': {'novalidate': 'novalidate'}} ) }}
    <table class="table-responsive">
        <tr>
            <td></td>
            <td><h1>ADD COMPETITION</h1></td>
            <td></td>
        </tr>
        <tr>
            <td>{{ form_label(f.nom,"Name") }}</td>
            <td>{{ form_widget(f.nom) }}</td>
            <td>{{ form_errors(f.nom) }}</td>
        </tr>
        <tr>
            <td>{{ form_label(f.date,"date") }}</td>
            <td>{{ form_widget(f.date) }}</td>
            <td>{{ form_errors(f.date) }}</td>
        </tr>
        <tr>
            <td>{{ form_label(f.description,"description") }}</td>
            <td>{{ form_widget(f.description) }}</td>
            <td>{{ form_errors(f.description) }}</td>
        </tr>
        <tr>
            <td>{{ form_label(f.capacite,"capacite") }}</td>
            <td>{{ form_widget(f.capacite) }}</td>
            <td>{{ form_errors(f.capacite) }}</td>
        </tr>
        <tr>
            <td>{{ form_label(f.videourl,"videourl") }}</td>
            <td>{{ form_widget(f.videourl) }}</td>
            <td>{{ form_errors(f.videourl) }}</td>
        </tr>
        <tr>
            <td>{{ form_label(f.fkOrganisateur,"fkOrganisateur") }}</td>
            <td>{{ form_widget(f.fkOrganisateur) }}</td>
            <td>{{ form_errors(f.fkOrganisateur) }}</td>
        </tr>
    </table>
    <div style="padding-left: 500px;">
    {{ form_widget(f.Submit) }}
    </div>
    {{form_end(f)}}
  </div>
 </div>
{% endblock %}