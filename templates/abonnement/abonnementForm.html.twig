{% extends 'admin_base.html.twig' %}
{% block body %}
 <div class="container-fluid pt-4 px-4">
    <div class="row g-4" style="padding-left: 50px;">
        <div class="col-sm-12 col-xl-10">
            <div class="bg-secondary rounded h-100 p-4">
                <h5 class="mb-4">Add Organizer</h5>
                {{ form_start(f,{'attr': {'novalidate': 'novalidate'}} ) }}
                
                    <div class="mb-3">
                        {{ form_label(f.montant,"montant") }}
                        {{ form_widget(f.montant) }}
                        {{ form_errors(f.montant) }}
                    </div>
                    <div class="mb-3">
                        {{ form_label(f.duree,"duree ") }}
                        {{ form_widget(f.duree) }}
                        {{ form_errors(f.duree) }}
                    </div>
                   
                    <div class="mb-3">
                        {{ form_label(f.description," description ") }}
                        {{ form_widget(f.description) }}
                        {{ form_errors(f.description) }}
                    </div>
                    <div style="padding-left: 350px;">
                        {{ form_widget(f.Submit) }}
                    </div>
                {{form_end(f)}}
            </div>
        </div>
    </div>
 </div>
{% endblock %}