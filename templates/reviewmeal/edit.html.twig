<!DOCTYPE html>

<title>Edit Reviewmeal</title>

{% block body %}
    <h1>Edit Reviewmeal</h1>

    {{ include('reviewmeal/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_reviewmeal_index') }}">back to list</a>

    {{ include('reviewmeal/_delete_form.html.twig') }}
{% endblock %}
